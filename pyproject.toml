[project]
name = "redmine-mcp"
version = "0.1.0"
description = "MCP server for Redmine project management"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "python-redmine>=2.5.0",
    "mcp>=1.6.0",
    "requests>=2.31.0",
    "python-dotenv>=1.0.0",
]
